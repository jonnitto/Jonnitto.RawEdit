namespace: Fusion = Neos.Fusion
namespace: Raw = Jonnitto.RawEdit

// Make this variable everywhere available
root.@context {
	rawContent = ${node.context.currentRenderingMode.name == 'rawContent' ? true : false}
}

include: Prototypes/**/*.fusion
